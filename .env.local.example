# Copy to .env.local for local dev, and set the same vars in your host (Railway) project settings.
# Values are in C:\morningpower\.env.tiktok (private).
TIKTOK_CLIENT_KEY=your_client_key
TIKTOK_CLIENT_SECRET=your_client_secret
